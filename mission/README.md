# Mission content root

This directory is the markdown-backed source of truth for Mission Control operating records.

## Initial scaffold
- `inbox/voice/` for reviewed voice intake records
- `departments/*/tasks/` for department task records
- `decisions/`, `approvals/`, `exceptions/`, and `shipped/` for published records

This first Voice Mode foundation PR scaffolds the tree before transcript review and markdown publish flows land.
